#include "collect_coin.h"

std::vector<point> coin_collecter::get_path(MAZE **newinformation = nullptr)
{
    point b;
    for (int i = 0; i < 3; i++)
    {
        for (int j = 0; j < 3; j++)
        {
            wait();//暂时放这里
        }
    }
}